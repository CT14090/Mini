# Core document processing
docling>=1.0.0
python-dotenv>=1.0.0

# Computer vision and image processing  
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# LLM integration for characteristic-specific feedback
langchain-openai>=0.1.0
langchain-core>=0.2.0
langchain>=0.2.0

# Web interface for viewing results
streamlit>=1.28.0

# HTTP requests for URL downloading
requests>=2.31.0

# Standard library dependencies (no install needed)
# json, os, pathlib, sqlite3, sys, subprocess, time, hashlib, argparse
# io, base64, tempfile, datetime, typing, dataclasses, enum

# Optional dependencies for enhanced functionality  
# azure-openai>=1.0.0  # Alternative client if langchain has issues